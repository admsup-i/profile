apiVersion: v1
kind: Service
metadata:
  name: profile
  namespace: production
spec:
  type: NodePort
  selector:
    app: profile
  ports:
    - port: 8082
      targetPort: 8082
      nodePort: 30081
